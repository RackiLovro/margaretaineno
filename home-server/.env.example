# Port the storage server listens on (Cloudflare Tunnel points here).
PORT=8787

# Shared secret. MUST match STORAGE_SECRET on Vercel. Use a long random string.
STORAGE_SECRET=replace-with-a-long-random-secret-string